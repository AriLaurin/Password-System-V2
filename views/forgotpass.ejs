<%- include('partials/header'); -%>

<h1>forgot pass page</h1>
<p>Enter in your email that you forgot the password to:</p>
<form action="">
    <label for="email">Email</label>
    <input type="text" name="email" required>
</form>

<script>
   const form = document.querySelector("form");
   const para = document.querySelector("p");

    //get values of email and pass
    let email = form.email.value;



        form.addEventListener("submit", async (e) => {
            e.preventDefault();
            let email = form.email.value;
            para.innerHTML = `an email has been sent to ${email} to verify that it is you.` 
            form.reset();

            try{ 
            const saveData = await fetch("/forgotpass", {
                method: "POST",
                body: JSON.stringify({data: email}),
                headers: {"Content-Type": "application/json"}
            });
            const emaildata = await saveData.json();
            // console.log(emaildata);
            
        }
        
        catch(err){
            console.log(err);
        }
        });



        
        
</script>

<%- include('partials/footer'); -%>